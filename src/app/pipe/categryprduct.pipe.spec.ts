import { CategryprductPipe } from './categryprduct.pipe';

describe('CategryprductPipe', () => {
  it('create an instance', () => {
    const pipe = new CategryprductPipe();
    expect(pipe).toBeTruthy();
  });
});
